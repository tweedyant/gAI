{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist"
        }
      }
    ],
    "build": {
      "env": {
        "NODE_ENV": "production"
      }
    },
    "buildCommand": "pnpm start --characters=\"characters/gai.character.json\" && pnpm run dev"
  }
